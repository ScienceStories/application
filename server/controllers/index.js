const story = require('./story');
const wikidata = require('./wikidata');
const annotation = require('./annotation');
const aws = require('./aws');
const google = require('./google');
module.exports = {story, wikidata, annotation, aws, google};
